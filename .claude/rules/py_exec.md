---
paths: **/*.py
---

# Python Dev Rules

- Always activate the virtual environment (if there is one) with `source venv/bin/activate` before running Python scripts to ensure correct dependency resolution.
